<div class='col-md-6 col-md-offset-3'>
  <h3>Categorias de Productos</h3>
  <ol>
    <% @categories.each do |category| %>
      <li><%= category.name %>
      <%= link_to 'Editar', edit_admin_category_path(category) %></li>
    <% end %>
  </ol>
</div>